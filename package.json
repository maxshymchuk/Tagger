{
    "name": "tagger",
    "scripts": {
        "build:fe": "cd client && npm i && npm run build",
        "build:be": "cd server && npm i && npm run build",
        "build": "npm run build:be && npm run build:fe"
    }
}